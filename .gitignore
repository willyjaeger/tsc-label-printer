# PyInstaller output
dist/
build/
*.spec

# Config con IP guardada (cada PC tiene la suya)
config.json

# Datos locales de pedidos (cada PC tiene los suyos)
orders.json

# Claude Code settings locales
.claude/settings.local.json

# Backend: config con credenciales reales (no subir al repo)
backend/config.php

# Python cache
__pycache__/
*.pyc
*.pyo

# VS Code (solo settings locales, no el workspace compartido)
.vscode/*.code-workspace
