# PyInstaller output
dist/
build/
*.spec

# Config con IP guardada (cada PC tiene la suya)
config.json

# Python cache
__pycache__/
*.pyc
*.pyo

# VS Code (solo settings locales, no el workspace compartido)
.vscode/*.code-workspace
